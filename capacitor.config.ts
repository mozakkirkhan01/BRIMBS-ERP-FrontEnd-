import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.school.app',
  appName: 'school',
  webDir: 'dist/school-erp',
  bundledWebRuntime: false
};

export default config;
